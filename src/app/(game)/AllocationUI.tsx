import React from 'react';

import AllocationUI from '../../screens/AllocationUI';

// "/AllocationUI" route (inside the (game) group).
export default function AllocationUIRoute() {
  return <AllocationUI />;
}
