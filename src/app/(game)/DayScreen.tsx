import React from 'react';

import DayScreen from '../../screens/DayScreen';

// "/DayScreen" route — the whole day-by-day career experience.
export default function DayScreenRoute() {
  return <DayScreen />;
}
