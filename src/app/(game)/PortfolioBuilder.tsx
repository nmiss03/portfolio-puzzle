import React from 'react';

import PortfolioBuilder from '../../screens/PortfolioBuilder';

// "/PortfolioBuilder" route (inside the (game) group).
export default function PortfolioBuilderRoute() {
  return <PortfolioBuilder />;
}
