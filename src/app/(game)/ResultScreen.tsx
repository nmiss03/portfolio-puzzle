import React from 'react';

import ResultScreen from '../../screens/ResultScreen';

// "/ResultScreen" route (inside the (game) group).
export default function ResultScreenRoute() {
  return <ResultScreen />;
}
