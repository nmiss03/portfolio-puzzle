import React from 'react';

import StockDashboard from '../../screens/StockDashboard';

// "/StockDashboard" route (inside the (game) group).
export default function StockDashboardRoute() {
  return <StockDashboard />;
}
