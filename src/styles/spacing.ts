// 4px-grid spacing scale — use these instead of arbitrary values.
export const space = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
} as const;

// Pixel-UI constants.
export const BORDER = 2; // 2px solid borders on cards/buttons/inputs
export const RADIUS = 0; // square corners everywhere
