// Single source of truth for the build's public version. Shown on the title
// screen, in Settings, and in the desktop footer. Bump this on each release.
export const GAME_VERSION = '0.1.0';
export const VERSION_LABEL = `ALPHA v${GAME_VERSION}`;
